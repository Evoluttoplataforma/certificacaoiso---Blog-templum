# Copie para .env (que o .gitignore já cobre: ".env" e ".env.*") e preencha.
# Uso:  node --env-file=.env scripts/audio-resumo.mjs --seco
#
# service_role: IGNORA A RLS. Só neste arquivo local, nunca no git, nunca em chat.
SUPABASE_SERVICE_KEY=
ELEVENLABS_API_KEY=
